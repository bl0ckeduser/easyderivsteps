#filter substitution
[App]
;
; This field specifies your organization's name.  This field is recommended,
; but optional.
;Vendor=
;
; This field specifies your application's name.  This field is required.

; NOTE: this is a hack because of sloppy browser detection in mathjax.js.
; I have to this as "name" because that's what XULRunner passes as a UA
; string to mathjax.js. And mathjax.js will decide not to work optimally
; if the UA string passed is not its table of "minimum browsers". What a mess.
Name=MSIE

; This field specifies your application's version.  This field is required.
; NOTE: This is a gain a hack. mathjax.js expects MSIE >= 6.0.
Version=9000.0
;
; This field specifies your application's build ID (timestamp).  This field is
; required.
BuildID=20130608
;
; This field specifies a compact copyright notice for your application.  This
; field is optional.
Copyright=Copyright (c) 2013, bl0ckeduser
;
; This ID is just an example.  Every XUL app ought to have it's own unique ID.
; You can use the microsoft "guidgen" or "uuidgen" tools, or go on
; irc.mozilla.org and /msg botbot uuid.  This field is optional.
;ID={3aea3f07-ffe3-4060-bb03-bff3a5365e90}

[Gecko]
;
; This field is required.  It specifies the minimum Gecko version that this
; application requires.
MinVersion=13.0.0
;
; This field is optional.  It specifies the maximum Gecko version that this
; application requires.  It should be specified if your application uses
; unfrozen interfaces.
MaxVersion=200.*

[Shell]
;
; This field specifies the location of your application's main icon with file
; extension excluded.  NOTE: Unix style file separators are required.  This
; field is optional.
;Icon=chrome/icons/default/simple

